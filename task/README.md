# Backend API

Node.js, Express, Sequelize, PostgreSQL.

## Setup

```bash
npm install
cp .env.example .env
# Edit .env with your DB and Cloudinary values
node server.js
```

Or `npm run dev` with nodemon.

## Environment

See `.env.example` for `DB_*`, `CLOUDINARY_*`, and `JWT_SECRET`.
